PORT=3000
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/railway
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Willkommens-DM nach Registrierung (nur mit CardCore-Schema: listing, conversation, message, Spalte is_welcome_anchor). Nur "0" schaltet ab.
# WELCOME_DM_ENABLED=1

# Social-Links (Profil): Spalten is_admin / social_network_enabled (Migration 015).
# Freischaltung z. B.: UPDATE app_user SET social_network_enabled = TRUE WHERE id = …;
# Admins: UPDATE app_user SET is_admin = TRUE WHERE email = '…';

# Push (Expo). Abschalten: =0
# PUSH_NEW_LISTING_ENABLED=0
# PUSH_NEW_MESSAGE_ENABLED=0

# Greenfield-DB: Migration sql/016_marketplace_messaging_core.sql vor sql/014_review_public_api.sql ausführen,
# falls noch keine listing-Tabelle existiert (FK in review).
